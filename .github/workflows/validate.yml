name: Validate

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-build:
    name: Test and build (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14", "3.15-dev"]
    steps:
      - uses: actions/checkout@v7
      - uses: deeprave/python-uv-actions/test-build@v1
        with:
          python-version: ${{ matrix.python-version }}
